apply plugin: "war"

bootWar {
    mainClassName = "com.icthh.xm.tmf.ms.order.OrderApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
